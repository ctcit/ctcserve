<VirtualHost *:443>
	# Redirect www to non-www
	# Traffic on www.ctc.org.nz:80 (non-https) is handled by
	# the 000-default.conf
        ServerName www.ctc.org.nz
        Redirect permanent / http://ctc.org.nz/
</VirtualHost>
